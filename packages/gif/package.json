{
  "name": "@jsquash/gif",
  "version": "1.0.0",
  "main": "dist/index.js",
  "description": "Wasm GIF decoder supporting the browser. Decode GIF images to ImageData.",
  "repository": "jamsinclair/jSquash",
  "author": {
    "name": "Jamie Sinclair",
    "email": "jamsinclairnz+npm@gmail.com"
  },
  "keywords": [
    "image",
    "optimisation",
    "optimization",
    "squoosh",
    "wasm",
    "webassembly",
    "gif"
  ],
  "license": "Apache-2.0",
  "scripts": {
    "clean": "rm -rf dist",
    "build:codec": "cd codec && npm run build",
    "build": "npm run clean && tsc && cp -r codec package.json ../../LICENSE dist && cd dist/codec",
    "prepublishOnly": "[[ \"$PWD\" == *'/dist' ]] && exit 0 || (echo 'Please run npm publish from the dist directory' && exit 1)"
  },
  "devDependencies": {
    "@types/node": "^20.9.2",
    "typescript": "^4.4.4"
  },
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./dist/index.js",
    "./*": "./dist/*"
  }
}